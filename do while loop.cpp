#include <stdio.h>
int main()
{
    int num,sum;
    do
    {
        printf("\num:,%d",num);
        sum=sum+num;
    }
    while(num!=0);
    {
        printf("sum:%d,",sum);
    }
        return 0;
    }

